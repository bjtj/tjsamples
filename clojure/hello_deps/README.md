# tools deps

## Execute function

``` shell
$ clj -X hello.core/hello
```

with parameter

``` shell
clj -X hello.core/hello :name Alice
```


## Using alias

Invoke main

``` shell
clj -M:run-m
```

Invoke function

``` shell
clj -X:run-x
```

<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/nav_graph"
  app:startDestination="@id/startFragment">

  <fragment
    android:id="@+id/startFragment"
    android:name="com.example.navigationexample.fragments.StartFragment"
    android:label="fragment_start">
    <action
      android:id="@+id/action_start_to_menu"
      app:destination="@id/menuFragment"
      app:popUpTo="@id/startFragment"
      app:popUpToInclusive="true" />
  </fragment>
  <fragment
    android:id="@+id/menuFragment"
    android:name="com.example.navigationexample.fragments.MenuFragment"
    android:label="fragment_menu"
    tools:layout="@layout/fragment_menu" >
    <action
      android:id="@+id/action_menu_to_content"
      app:destination="@id/contentFragment" />
  </fragment>
  <fragment
    android:id="@+id/contentFragment"
    android:name="com.example.navigationexample.fragments.ContentFragment"
    android:label="fragment_content"
    tools:layout="@layout/fragment_content" >
    <argument
      android:name="content"
      app:argType="string" />
    <action
      android:id="@+id/action_contentFragment_to_startFragment"
      app:destination="@id/startFragment"
      app:popUpTo="@id/contentFragment"
      app:popUpToInclusive="true" />
  </fragment>
</navigation>
